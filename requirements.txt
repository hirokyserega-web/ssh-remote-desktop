# Cross-platform runtime dependencies.
# Installed on every OS (Linux, Windows, macOS).
#
# Kept in sync with pyproject.toml [project.dependencies] + cross-platform
# extras. tests/test_dependency_consistency.py enforces that the lower bounds
# here match pyproject, so there is a single source of truth.
asyncssh>=2.23
cryptography>=44
bcrypt>=4.2
numpy>=2.2
msgpack>=1.1
pillow>=11
python-xlib>=0.33
mss>=10.0
av>=14.0
PySide6>=6.8

# Linux-only runtime dependencies (keyboard/evdev + Wayland + D-Bus)
# live in requirements-linux.txt — they ship no Windows/macOS wheels, so
# keeping them here would break `pip install -r requirements.txt` on
# non-Linux OSes. On Linux, install both:
#   pip install -r requirements.txt -r requirements-linux.txt
